# MASD: SSL Pretraining Configuration
data_dir: "data/LibriSpeech/train-clean-360"
output_dir: "checkpoints/ssl_pretrained"

epochs: 100
batch_size: 256
lr: 0.0001
weight_decay: 0.01
fp16: true
num_gpus: 4
